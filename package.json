{
  "name": "meteoblue-simulation-collector",
  "version": "0.0.1",
  "description": "collect simulated archive data from meteoblue",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Clemens Solar",
  "dependencies": {
    "eslint": "^8.6.0",
    "puppeteer": "^13.0.1"
  },
  "devDependencies": {
    "eslint-config-google": "^0.14.0"
  }
}
