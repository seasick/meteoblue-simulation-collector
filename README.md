# meteoblue-simulation-collector

This repository collects simulated daily data (with hourly resolution) from Meteoblue.

The data can be found in the `./data` directory and contains the data in daily files.
